export const environment = {
  production: true,
  apiUrl: 'https://lukafranjic.xyz/api/kenaz',
};

export enum ApiPaths {
  Users = '/users',
  Articles = '/articles',
  Categories = '/categories',
  Comments = '/comments',
}
