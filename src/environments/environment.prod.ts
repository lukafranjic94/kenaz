export const environment = {
  production: true,
  apiUrl: 'http://localhost:3000/api',
};

export enum ApiPaths {
  Users = '/users',
  Articles = '/articles',
  Categories = '/categories',
  Comments = '/comments',
}
